version: "2"

linters:
  default: standard
  enable:
    - asciicheck
    - bidichk
    - copyloopvar
    - dupword
    - durationcheck
    - errname
    - errorlint
    - gocritic
    - godoclint
    - godot
    - goprintffuncname
    - gosec
    - intrange
    - misspell
    - nakedret
    - nilerr
    - noctx
    - prealloc
    - revive
    - unconvert
    - unparam
    - wastedassign

  exclusions:
    rules:
      # SHA-1 is required by RFC 9562 for V5 UUIDs, not used for security.
      - linters: [gosec]
        rules: [G505]
        path: generate\.go

  settings:
    godot:
      scope: toplevel
    nakedret:
      max-func-lines: 0
    gocritic:
      enabled-tags:
        - diagnostic
        - performance
        - style
